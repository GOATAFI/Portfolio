import FullScreen from "../../components/layout/FullScreen";
import Quote from "../../components/ui/Quote";

export default function ThenWhy() {
  return (
    <FullScreen>
      <Quote className="text-5xl md:text-7xl">
        Then why?
      </Quote>
    </FullScreen>
  );
}